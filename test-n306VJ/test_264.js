let mocha = require('mocha');
let assert = require('assert');
let js_code = require('..');
describe('test suite', function() {
    it('test case', function(done) {
        //assert.equal(js_code.computeSumOfPrimes([2, 3, 5, 7]), 17, 'computeSumOfPrimes([2, 3, 5, 7]) should return 17');
        //assert.equal(js_code.computeSumOfPrimes([11]), 11, 'computeSumOfPrimes([11]) should return 11');
        done();
    })
})